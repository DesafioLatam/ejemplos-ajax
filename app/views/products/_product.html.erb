<div id="product-<%= product.id %>">
  <div class="thumbnail col-md-3 products">
    <%= product.name %>
  <br>

  <%= link_to 'Edit', edit_product_path(product), remote: true %>
  <%= link_to 'Destroy', product, method: :delete, data: {disable_with: "<h2> Esperando que Mati pare de conversar</h2>"}, remote:true  %>
  </div>
</div>
